<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('default.edit'), edit_book_path(@book) %> |
  <%= link_to t('default.back'), books_path %>
  <%= button_to t('default.delete'), @book, method: :delete %>
</nav>
